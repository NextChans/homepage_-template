/**
 * 브랜드 아이덴티티 — 심볼 도형과 아이덴티티 색.
 *
 * ## 왜 `content/` 에 있는가
 *
 * 로고 도형은 **데이터**다. 컴포넌트(`components/brand-mark.tsx`)와 파비콘·OG
 * 이미지 생성 스크립트(`scripts/generate-brand-assets.mjs`)가 **같은 값을** 써야
 * 한다. 두 곳에 복사해 두면 심볼을 다듬을 때 한쪽만 바뀐다.
 *
 * ## ⚠️ 색상 리터럴 금지 규칙의 예외다
 *
 * `CLAUDE.md` 는 색상 리터럴(`#hex`)을 금지하고 `@theme` 토큰만 쓰게 한다. 그
 * 규칙의 목적은 **UI 색이 여러 곳에서 갈리는 것**을 막는 것이다. 아래 두 색은
 * UI 색이 아니다 —
 *
 *  - 화면(헤더·푸터)의 심볼은 `currentColor` 로 그린다. **아이덴티티 색을 쓰지
 *    않는다.** 사이트는 화이트+`accent` 단색 체계를 유지한다(팔레트 (A)안).
 *  - 아래 색은 **래스터 아이덴티티 자산에만** 쓴다: 파비콘, 앱 아이콘, OG 이미지.
 *    이들은 PNG/SVG 파일이라 Tailwind 토큰을 참조할 수 없다.
 *
 * ⚠️ **`gold` 를 본문·링크·버튼에 쓰지 않는다.** 흰 배경 대비비가 **2.25:1** 로
 *    WCAG AA(본문 4.5:1, UI 3:1)에 미달한다. 큰 면적의 도형에만 쓴다.
 *    `navy` 는 16.2:1 로 텍스트에 써도 되지만, 사이트 강조색은 `accent` 하나다.
 */

/** 아이덴티티 색. 래스터 자산 전용 — 화면 UI 에 쓰지 않는다. 위 주석을 읽을 것. */
export const brandColors = {
  /** Deep Navy. 아이콘 배경. 흰 배경 대비 16.2:1 */
  navy: '#0A1F44',
  /** Secondary Blue. 아이콘 배경 그라데이션 하단 */
  navyDeep: '#1B3668',
  /** Gold. 심볼 도형 전용. 대비 2.25:1 — 텍스트 금지 */
  gold: '#C9A961',
} as const

/**
 * 심볼 — 방패(Shield of Trust).
 *
 * `viewBox="0 0 28 33"` 기준. **`fill-rule="evenodd"` 로 그려야 한다** —
 * 사선 2개가 서브패스로 들어 있고, evenodd 가 그것을 **구멍**으로 만든다.
 * `nonzero` 로 그리면 사선이 사라져 방패만 남는다.
 *
 * ## 왜 사선이 3개가 아니라 2개인가
 *
 * 원안은 3개였다. 파비콘 16px 에서 사선 3개는 **뭉쳐서 한 덩어리로 보인다**
 * (선과 간격이 각각 1px 미만이 된다). 2개로 줄이고 굵혀야 작은 크기에서 살아남는다.
 *
 * ## 왜 mask 가 아니라 evenodd 인가
 *
 * `<mask>` 는 `id` 가 필요하고, 헤더·푸터에서 심볼이 두 번 렌더되면 **중복 id** 가
 * 된다. `useId()` 로 풀려면 클라이언트 컴포넌트가 되어야 한다 — 심볼 하나 때문에
 * 서버 컴포넌트를 포기할 이유가 없다. evenodd 는 `<path>` 하나로 끝난다.
 *
 * ⚠️ 사선 평행사변형의 좌표는 **방패 내부에 완전히 들어가도록** 계산되어 있다.
 *    evenodd 는 도형 밖으로 나간 부분을 잘라주지 않고 **거기까지 칠한다.**
 *    좌표를 손대려면 방패 경계와의 여백(최소 2.4)을 다시 확인할 것.
 */
export const SHIELD_PATH =
  // 방패 외곽 — 윗변은 반경 2.4 라운드, 측면 수직, 아래는 (14, 30.4) 한 점으로 수렴
  'M4.9 2.6H23.1A2.4 2.4 0 0 1 25.5 5V16.4C25.5 22.8 20.7 27.9 14 30.4' +
  'C7.3 27.9 2.5 22.8 2.5 16.4V5A2.4 2.4 0 0 1 4.9 2.6Z' +
  // 사선 1 (구멍) — 45°, 좌하 → 우상
  'M13 9.5H16.2L8.2 17.5H5Z' +
  // 사선 2 (구멍)
  'M19.5 9.5H22.7L14.7 17.5H11.5Z'

/** 심볼 뷰박스. 컴포넌트와 자산 생성 스크립트가 공유한다. */
export const SHIELD_VIEWBOX = '0 0 28 33'
